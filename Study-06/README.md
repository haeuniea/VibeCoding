# Study-06 — 쇼핑 리스트 앱

서버 없이 브라우저에서 바로 실행되는 간단한 쇼핑 리스트 웹앱입니다.

## 기능

- 아이템 이름 + 수량 입력 후 추가
- 체크박스로 완료 표시 (취소선 처리)
- 삭제 버튼으로 즉시 삭제
- `localStorage`에 저장되어 새로고침/재접속해도 목록 유지

## 실행 방법

`index.html` 파일을 브라우저에서 열면 바로 사용할 수 있습니다. 별도 설치나 서버 실행이 필요 없습니다.
